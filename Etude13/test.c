#include <stdio.h>
#include <stdlib.h>

struct S {
    char *firstName;
    char *lastName;
    int phone;
    char *emailAddress;
};

int main() {
    /*
        Getting a string of characters
    */
    struct S** ss = (struct S**) malloc(100*sizeof(struct S**));
    return 0;
}

